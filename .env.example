# Copy to .env and adjust for your setup.
#
# Docker Compose (api container): use service hostnames postgres / redis
# Host-side uvicorn (make up for DB only): use localhost

DATABASE_URL=postgresql://shortlink:shortlink@postgres:5432/shortlink
REDIS_URL=redis://redis:6379/0
APP_ENV=development